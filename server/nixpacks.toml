[phases.setup]
nixPkgs = ['python312']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['python manage.py migrate --noinput', 'python manage.py collectstatic --noinput']

[start]
cmd = 'gunicorn djangoproj.wsgi --log-file -'
